@model mvcMovie.ViewModels.PhieuNhapView
@section body_scripts
{
    @Html.Partial("_LeftMenu.cshtml")
}
@section above_scripts
{
    @Html.Partial("_AboveMenu.cshtml")
}

<div class="card-body">
        <h3>Phiếu Nhập</h3>
                            <div class="table-responsive">
                                  <p>
                                    <a asp-action="ThemPhieuNhap">Create New</a>
                                </p>
                                <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                                    <thead>
                                        <tr>
                                            <th  scope="col">Id</th>
                                            <th  scope="col">Tên</th>
                                            <th  scope="col">Ngày Nhập</th>
                                            <th  scope="col">NhaCungCapId</th>
                                            <th scope="col"> </th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        @if (ViewBag.List != null) 
                                        {

                                        @foreach (var item in ViewBag.List)
                                        {
                                            <tr>
                                                <td>@item.Id</td>
                                                <td>@item.Ten</td>
                                                <td>@item.NgayNhap</td>
                                                <td>@item.NhaCungCapId</td>
                                                 <td>
                                                 <a asp-action="SuaPhieuNhap" asp-route-id="@item.Id">Edit</a> |
                                                 <a asp-action="ThongTinPhieuNhap" asp-route-id="@item.Id">Details</a> |
                                                 <a asp-action="XoaPhieuNhap" asp-route-id="@item.Id">Delete</a>
                                                 </td>
                                            </tr>
                                        }  
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
