@model mvcMovie.ViewModels.HoaDonView
@section body_scripts
{
    @Html.Partial("_LeftMenu.cshtml")
}
@section above_scripts
{
    @Html.Partial("_AboveMenu.cshtml")
}

<div class="card-body">
    <h3>Hóa Đơn</h3>
                            <div class="table-responsive">
                                  <p>
                                    <a asp-action="ThemHoaDon">Create New</a>
                                </p>
                                <table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">
                                    <thead>
                                        <tr>
                                            <th  scope="col">Id</th>
                                            <th  scope="col">Ngày Lập</th>
                                            <th  scope="col">KhachHangId</th>
                                            <th scope="col"> </th>
                                          
                                        </tr>
                                    </thead>

                                    <tbody>
                                        @if (ViewBag.List != null) 
                                        {

                                        @foreach (var item in ViewBag.List)
                                        {
                                            <tr>
                                                <td>@item.Id</td>
                                                <td>@item.NgayLap</td>
                                                <td>@item.KhachHangId</td>
                                                 <td>
                                                 <a asp-action="SuaHoaDon" asp-route-id="@item.Id">Edit</a> |
                                                 <a asp-action="ThongTinHoaDon" asp-route-id="@item.Id">Details</a> |
                                                 <a asp-action="XoaHoaDon" asp-route-id="@item.Id">Delete</a>
                                                 </td>
                                            </tr>
                                        }  
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
